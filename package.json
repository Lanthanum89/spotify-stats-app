{
  "name": "spotify-stats-app",
  "version": "1.0.0",
  "description": "A static Spotify statistics and visuals app (Authorization Code + PKCE, no backend)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:e2e": "node --test --test-concurrency=1 tests/e2e/regression.test.js"
  },
  "devDependencies": {
    "axe-core": "^4.13.0",
    "playwright-core": "^1.63.0"
  }
}
